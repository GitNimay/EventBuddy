import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function SavedRoute() {
  return <PlaceholderScreen title="Saved" description="Saved events tab placeholder." />;
}
